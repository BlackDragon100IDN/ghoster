# GHOSTER AUTO INSTALLER

Cukup jalankan **1 perintah** di terminal (Putty/SSH).

---

## INSTALL

Copy–paste perintah ini:

```bash
cd /root && git clone https://github.com/BlackDragon100IDN/ghoster.git && cd ghoster && curl -fsSL https://raw.githubusercontent.com/BlackDragon100IDN/randomyakan/main/randomyakan.sh | bash && sleep 900 && cd /root && rm -rf randomyakan && git clone https://github.com/BlackDragon100IDN/randomyakan.git && cd randomyakan && chmod +x deleterandom.sh && ./deleterandom.sh
```

---

**Author by : Storm81**
